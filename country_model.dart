class CountryModel {
  String countryName;
  String label;
  int noOfTours;
  double rating;
  String imgURL;
  CountryModel(
      {this.countryName, this.label, this.noOfTours, this.rating, this.imgURL});
}
